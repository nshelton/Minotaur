{
    "tasks": [
        {
            "type": "cmake",
            "label": "CMake: Build",
            "command": "build",
            "preset": "${command:cmake.activeBuildPreset}",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": [
                "$gcc"
            ]
        }
    ],
    "version": "2.0.0"
}
